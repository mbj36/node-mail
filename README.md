# node-mail

### Instructions

You need to create `config.json` file with following values

```
{
    "credentials": {
        "user":"",
        "password":"",
        "to":""
    }
}
```

`npm install`

`npm start`
